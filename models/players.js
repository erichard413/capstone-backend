"use strict";

const {NHLAPI_BASE_URL} = require('../apis/apis');
const axios = require('axios');

class Players {
    // this will get list of all players from API, format it for the front end.
    static async getPlayers() {
        try {
            
        } catch(err) {
            return next(err);
        }
    }
}

module.exports = Players;